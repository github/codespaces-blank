def decode (contents):
    # break into 4 character chunks
    # convert each chunk to a number
    i = 0
    while i < len(contents):
        contents[i] 
    while i % 4 != 0:
        contents[i] = contents[i] + '='
        i += 1
    
    # or the other option is to create a dictionary
    # and instead of using elif, use a dictionary
    bcd_to_dec = {"0000": }


def main():
    
    
if __name__ == '__main__':
    main()